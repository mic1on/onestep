# OneStep Agent WS Protocol Reference

The canonical Agent WS protocol now lives in `onestep-control-plane`:

- `../onestep-control-plane/docs/protocols/agent-ws-protocol.md`

This file remains only as a stable pointer from the `onestep` repo so local
notes and older links do not drift into a second protocol source.

For implementation sequencing, rollout notes, and cross-repo ownership, use:

- `docs/ws-cross-repo-collaboration.md`
