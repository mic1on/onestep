### Build ###
node_modules
.vitepress/cache
dist*
dist-ssr
*.local
auto-imports.d.ts
.eslintrc-auto-import.json
src/assets/sprites/*.*
!src/assets/sprites/.gitkeep
*.log
package-lock.json

# Local pnpm 10 build-script approval only. pnpm 9 (used in CI) treats this
# file as a workspace manifest and requires a `packages` field, so it must
# stay untracked.
pnpm-workspace.yaml

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### MAC ###
.DS_Store

### version check ###
version.json
