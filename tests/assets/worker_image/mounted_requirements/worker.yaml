app:
  name: mounted-requirements

resources:
  tick:
    type: interval
    seconds: 60
    immediate: true
    payload:
      source: mounted-requirements

tasks:
  - name: run_once
    source: tick
    handler:
      ref: worker_image_demo.tasks:run_once
