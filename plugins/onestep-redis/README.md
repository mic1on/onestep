# onestep-redis

Redis connector plugin for onestep.

```bash
pip install onestep-redis
```

The plugin registers YAML resource types through the `onestep.resources` entry point:

- `redis`
- `redis_stream`
